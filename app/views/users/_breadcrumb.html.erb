<ul class="breadcrumb">
	<b>Viewing:</b>
	<li class="active">
		<%= link_to @user.name, shop_user_path(@user) %> <span class="divider">/</span>
	</li>
	<li>
		Collections
	</li>
	<li class="pull-right">
		<% if current_user?(@user) %>
			<a href="#add-new-collection" data-toggle="modal" data-target="#add-new-collection" class="btn btn-small btn-success breadcrumb-btn">Add New Collection</a>
		<% end %>
	</li>
</ul>